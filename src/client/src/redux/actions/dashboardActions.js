export const SET_SELECTED_FLEET = (data) => {
  return {
    type: "SET_SELECTED_FLEET",
    payload: data,
  };
};

export const GET_SELECTED_FLEET = (data) => {
  return {
    type: "GET_SELECTED_FLEET",
    payload: data,
  };
};
